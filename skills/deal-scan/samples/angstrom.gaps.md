# Could not find — Angstrom (Sorella Labs)

## not-found

**Security audit: a named firm, a date, a scope and a linked report.** Searched
`api.llama.fi`, whose `audits` field reads 0 — a record of what DefiLlama holds, not a
statement about the protocol. The one audit-shaped search result, a store page at
`auditlabs.ai`, returned HTTP 404 when fetched, so nothing was recorded from it. Also
searched the `github.com/SorellaLabs/angstrom` contents listing. No report located, which
is not a statement that the code was never audited.

**Identity of the account that sent the creation transaction.** The explorer returns no
name, no ENS domain and a zero balance for it.

**Token, supply and unlock schedule.** Searched `api.llama.fi/protocol/angstrom` and
`angstrom.xyz`. No token found associated with this protocol in the sources read.
